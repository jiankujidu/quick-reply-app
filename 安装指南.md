# 快回复 Pro - Flutter APK 构建指南

## 第一步：下载 Flutter SDK

访问 Flutter 中文网下载页面：
https://flutter.cn/docs/get-started/install/windows

直接下载链接（推荐）：
https://storage.googleapis.com/flutter_infra_release/releases/stable/windows/flutter_windows_3.24.0-stable.zip

如果上面的链接无法访问，使用国内镜像：
https://mirrors.tuna.tsinghua.edu.cn/flutter_infra_release/releases/stable/windows/

---

## 第二步：安装 Flutter

1. 下载完成后，解压 `flutter_windows_3.24.0-stable.zip`
2. 将解压后的 `flutter` 文件夹移动到 `D:\flutter`
3. 最终路径应该是：`D:\flutter\bin\flutter.bat`

---

## 第三步：配置环境变量（可选但推荐）

1. 右键"此电脑" → "属性" → "高级系统设置"
2. 点击"环境变量"
3. 在"系统变量"中找到 `Path`，点击"编辑"
4. 添加：`D:\flutter\bin`
5. 添加国内镜像（重要！）：
   - 变量名：`PUB_HOSTED_URL`
   - 变量值：`https://pub.flutter-io.cn`
   - 变量名：`FLUTTER_STORAGE_BASE_URL`
   - 变量值：`https://storage.flutter-io.cn`

---

## 第四步：验证安装

打开 PowerShell 或 CMD，运行：

```bash
flutter --version
```

如果显示版本号（如 Flutter 3.24.0），说明安装成功！

---

## 第五步：构建 APK

打开 PowerShell，进入项目目录：

```bash
cd D:\桌面\quick_reply_app
```

运行构建命令：

```bash
flutter pub get
flutter build apk --debug
```

---

## 第六步：获取 APK

构建成功后，APK 文件位置：
```
D:\桌面\quick_reply_app\build\app\outputs\flutter-apk\app-debug.apk
```

你可以把它重命名为 `快回复Pro.apk`

---

## 常见问题

### Q: flutter pub get 失败
A: 确保设置了国内镜像环境变量，或使用 VPN

### Q: 找不到 Android SDK
A: 安装 Android Studio，或手动下载 Android SDK

### Q: 构建失败
A: 运行 `flutter doctor` 查看具体问题

---

## 项目信息

- **项目位置**: D:\桌面\quick_reply_app
- **构建脚本**: D:\桌面\quick_reply_app\build-apk.bat
- **主要代码**: D:\桌面\quick_reply_app\lib\main.dart
